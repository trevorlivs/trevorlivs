Trevor Livs integration coded for TLFeature1

Iss10 Merge

Feature Karate Added

Feature A added
public string getName(string name)
{
  if (name == "" ) {name = "trev");
  name = name.ucase(name);
  return name;
}


added on mater branch
Added this in my testing branch
